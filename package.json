{
  "scripts": {
    "start": "cd server && npm install && nodemon index.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^1.8.0",
    "client": "^0.0.1",
    "react-redux": "^7.2.6"
  }
}
